# ================================
# ⚙️ CẤU HÌNH BUILD CHO NETLIFY
# ================================

[build.environment]
  NODE_VERSION = "20"
  NETLIFY_USE_YARN = "true"
  YARN_VERSION = "1.22.17"

[build]
  base = "site"
  command = "yarn install && yarn run build"
  publish = "../dist"   # ⚠️ xuất ra thư mục dist ngoài thư mục site

# ================================
# 🧪 Cấu hình cho preview build
# ================================
[context.deploy-preview]
  command = "yarn install && yarn run build:preview"

# ================================
# 🧑‍💻 Cấu hình chạy Netlify Dev (local)
# ================================
[dev]
  framework = "hugo"
  targetPort = 1313
